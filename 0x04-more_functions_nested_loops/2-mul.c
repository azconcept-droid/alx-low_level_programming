#include "main.h"
/**
 * mul - multiplies two ints.
 * @a: first number.
 * @b: second number.
 *
 * Description: multiplies two numbers
 * Return: product.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
